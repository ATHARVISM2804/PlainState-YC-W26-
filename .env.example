# Base URL of the Plainstate ingestion API. Leave unset and the "try it"
# panel explains how to run the reader locally instead of pretending to work.
#
#   local:  VITE_API_URL=http://localhost:8000
#   prod:   VITE_API_URL=https://api.plainstate.com
VITE_API_URL=
